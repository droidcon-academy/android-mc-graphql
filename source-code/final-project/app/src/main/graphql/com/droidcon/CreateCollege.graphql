mutation CreateCollege($name: String!, $location:String!, $establishedYear:String!, $profileUrl:String!) {
    createCollege(establishedYear: $establishedYear, location: $location, name: $name, profileUrl: $profileUrl) {
        establishedYear
        id
        location
        name,
        profileUrl
    }
}